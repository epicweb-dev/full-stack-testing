# Playwright

It could help to have the <InlineFile file="app/routes/users+/index.tsx" /> file
open as a reference as you try to write the test.

Write test

Run

```sh nonumber
npx playwright test --ui
```
