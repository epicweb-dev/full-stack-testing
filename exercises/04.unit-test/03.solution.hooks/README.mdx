# Hooks

👨‍💼 Great, now we don't need to worry about any of the other tests using
`console.error` and mucking up the output of our tests. But what if something
calls `console.error` and we weren't expecting it? What if there was a real
error?! We'd completely miss it!

Let's address that next.
